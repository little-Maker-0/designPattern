迭代器模式提供一种方法顺序访问每一个聚合对象（集合）中的各个元素，而又不暴露其内部的表示。迭代器模式把在元素之间游走的任务交给迭代器，而不是聚合对象。

具体实现；每一个具体聚合都要负责实例化一个具体迭代器，也就是实现一个静态的createIterator方法。

单一责任原则：一个类应该只有一个引起变化的原因。类的每个责任都有改变的可能。超过一个责任，意味着超过一个改变的区域。改变，又一次成为中心。

补充：内聚，用来度量一个类或模块紧密地达到单一目的或责任。